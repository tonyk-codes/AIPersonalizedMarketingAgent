# Smart Nike Shoe Ad Studio - optional stage selection metadata.
#
# This file is kept for human-readable configuration notes.
# Runtime model ids and environment flags are managed in config.py and .env.

profile_encoder:
  # Options: hf, mock
  backend: hf

product_encoder:
  # Options: hf, mock
  backend: hf

copy_generator:
  # Options: hf, mock
  backend: hf

video_generator:
  # Options: hf, mock
  backend: hf
